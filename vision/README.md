# Vision module contract

Vision code's only required output is `inventory.json` in repo root.

Format:
{
  "<tray name>": <current_count_int>,
  ...
}

Tray names must match config/layout.json exactly.
